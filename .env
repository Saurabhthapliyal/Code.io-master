# code.io (Create React App)
# https://create-react-app.dev/docs/adding-custom-environment-variables/
#
# Only names starting with REACT_APP_ are exposed to the browser via process.env.
# This app does not use any yet — uncomment or add variables when you need them.

# REACT_APP_API_URL=
# REACT_APP_TITLE=code.io

# Dev server (optional)
# PORT=3000
# BROWSER=none
# HOST=0.0.0.0

# Use .env.local for machine-specific overrides (already in .gitignore).
